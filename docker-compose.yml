version: '3'
services:
  southy:
    build: ./src
    container_name: 'southy'
    tty: true
    volumes:
      - type: bind
        source: ./src
        target: /work
      - type: volume
        source: packages
        target: /usr/local/lib/python3.12/site-packages

volumes:
  packages: